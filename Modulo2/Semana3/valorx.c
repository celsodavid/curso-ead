#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */
/* exec 5 */
/* Reposta: Apenas a afirmativa II está correta. */
int main(int argc, char *argv[]) {
	int x=0;
	while (x<10) {
		printf("\nO valor de X eh %d", x);
	}
	system("PAUSE");
	return 0;
}
